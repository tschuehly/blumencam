# Blumencam Configuration

# Telegram Bot Configuration
# Replace with your actual bot token from BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here
# Replace with your actual chat ID
TELEGRAM_CHAT_ID=your_chat_id_here

# Image Configuration
# Directory where images will be saved
IMAGE_DIR=images
# Camera device index (usually 0 for the first webcam)
CAMERA_DEVICE=0
# Camera resolution (width x height in pixels)
CAMERA_WIDTH=1280
CAMERA_HEIGHT=720
# Camera exposure (-1 for auto exposure, or specific value depending on your camera)
CAMERA_EXPOSURE=5
# Camera brightness (0-100, default: 50)
CAMERA_BRIGHTNESS=50

# Scheduling and Failsafe Configuration
# Comma-separated list of times to capture images (24-hour format)
CAPTURE_TIMES=10:00,14:00,18:00
# Maximum number of retries for camera capture
MAX_RETRIES=3
# Delay between retries in seconds
RETRY_DELAY=5
